<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.norm.timemall.app.mall.mapper.MallRcLibraryMapper">


    <select id="selectRcPage" resultType="com.norm.timemall.app.mall.domain.ro.MallGetRcListRO">
        select rc.title,rc.thumbnail,rc.download_uri,rc.preview_uri
        from rc_library rc
        <where>
            1=1
            <if test="dto.q != null and dto.q != ''">
                AND locate(#{dto.q}, rc.title)>0
            </if>
        </where>
    </select>


</mapper>