<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.norm.timemall.app.ms.mapper.MsGroupMsgMapper">

    <select id="selectEventPage" resultType="com.norm.timemall.app.ms.domain.pojo.MsDefaultEventCard">
        select m.msg_id as id,b.brand_name author,b.avator authorIcon,m.author_id,m.create_at,m.msg_type,m.msg
        from group_msg m
        left join brand b
        on b.customer_id = m.author_id
        where m.channel_id = #{channel_id}
        and m.channel_type= #{channel_type}
        order by m.create_at desc
    </select>

</mapper>