<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.norm.timemall.app.pod.mapper.PodCellPlanOrderMapper">

    <select id="selectCellPlanOrderPage" resultType="com.norm.timemall.app.pod.domain.ro.PodCellPlanOrderPageRO">
        select o.id orderId,c.title cellTitle,o.plan_type_desc,o.plan_price price,o.tag,o.create_at
        from cell_plan_order o
        inner join cell c
           on c.id=o.cell_id
        where 1=1
            and o.consumer_id=#{consumer_id}
        <if test="dto.q != null and dto.q != ''">
            AND locate(#{dto.q}, c.title)>0
        </if>
        <if test="dto.planType != null and dto.planType != ''">
            AND o.plan_type=#{dto.planType}
        </if>
        <if test="dto.tag != null and dto.tag != ''">
            AND o.tag=#{dto.tag}
        </if>
    </select>



</mapper>