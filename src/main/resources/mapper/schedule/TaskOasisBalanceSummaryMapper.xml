<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.norm.timemall.app.scheduled.mapper.TaskOasisBalanceSummaryMapper">
    <sql id="tableName">
        oasis_balance_summary
    </sql>

    <update id="doExecuteProcedure" statementType="CALLABLE">
        {
           CALL cal_batch_generate_balance_summary_proc_0023()
        }
    </update>

</mapper>